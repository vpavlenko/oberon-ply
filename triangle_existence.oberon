MODULE triangle_existense;

VAR
  a, b, c: integer;

BEGIN
  read(a);
  read(b);
  read(c);
  if a + b > c and a + c > b and b + c > a the
    writeln(1)
  else
    writeln(0);
END triangle_existense.
